{
  "name": "duhocnb-workspace",
  "private": true,
  "scripts": {
    "verify:permissions-all": "npm --prefix backend run migrate && npm --prefix backend run seed:settings && npm --prefix backend run test:permissions && npm --prefix frontend run test:permission-middleware"
  }
}