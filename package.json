{
  "name": "duhocnb-workspace",
  "private": true,
  "scripts": {
    "install:all": "npm --prefix backend install && npm --prefix frontend install",
    "setup:db": "npm --prefix backend run db:setup",
    "setup:full": "npm run install:all && npm run setup:db",
    "verify:permissions-all": "npm --prefix backend run migrate && npm --prefix backend run seed:all && npm --prefix backend run test:permissions && npm --prefix frontend run test:permission-middleware"
  }
}